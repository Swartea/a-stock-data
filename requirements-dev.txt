# 开发/测试依赖
# 配合 requirements.txt 使用: pip install -r requirements.txt -r requirements-dev.txt
# 或用 pyproject.toml: pip install -e ".[test,dev]"

# test
pytest>=8.0,<10.0
pytest-cov>=4.0,<7.0
pytest-mock>=3.10,<4.0

# dev
ruff>=0.5,<1.0
black>=24.0,<26.0
mypy>=1.10,<2.0
